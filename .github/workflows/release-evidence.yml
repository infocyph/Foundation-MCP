name: "Foundation MCP Release Evidence"

on:
  push:
    branches:
      - main
      - "feature/**"
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    name: "Workflow validation smoke"
    runs-on: ubuntu-latest
    steps:
      - run: php -v
